import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tabContainer: {
    width: 350,
  },
  title: {
    alignSelf: 'center',
  },
  filterIcon: {
    position: 'absolute',
    right: 0,
  },
});

export default styles;
